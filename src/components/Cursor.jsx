import { useState, useEffect, useRef } from "react";

const Cursor = ({ i }) => {
    return <div
        id="cursor"
        className="transition-all w-[3px] h-[36px] bg-yellow-300 rounded-sm z-20 absolute"
        style={{

        }}
    />
}

export default Cursor;